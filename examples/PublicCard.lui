<!-- 中文公开属性来自同名后端的 Properties；布局参数由框架提供。 -->
<控件 名称="PublicCard" 副名称="公开属性卡片" 最小宽度="240" 内边距="8">
  <卡片 子项排列="垂直" 垂直间隔="8">
    <文本 文本="{绑定 props['标题'], 模式=单向, 更新源触发=默认}" 字号="20" />
    <内容呈现器 />
    <按钮 文本="确认" 点击="{绑定 props['确认'], 模式=单向, 更新源触发=默认}" />
  </卡片>
</控件>
