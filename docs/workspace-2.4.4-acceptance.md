# LUI Studio / Runtime 2.4.4 验收

## 范围

- 按钮新增`悬停背景`、`按下背景`，共享词汇、补全、属性栏、Parser和UrhoX Runtime使用同一映射。
- 显式`背景`在未声明交互背景时沿用到悬停与按下状态，避免底层主题覆盖设计色。
- 当时 Runtime 复合文字测量采用1.2逻辑行高；该值后来确认与 Studio 的1.45不一致，2.4.5已废止。布局仍先排除边框和内边距。
- 保留Studio的100%初始倍率、直角选中框和折叠节点不占位行为。

## 自动验证

- `npm test`：61项通过。
- `npm run check:types`：通过。
- `npm run check:docs`：86个链接、7段代码、3组文档对、5个Lua片段通过。
- `npm run check:lua -- D:\项目\Tap制造\无尽塔`：55份Lua语法通过。
- `tests/runtime-layout-math.py`：按钮继承、自由流填充、8px滚动条槽、30帧稳定性及声明不可变通过。
- 游戏侧48件品质、17份设计结构、267条绑定/动作、页面视图、16组纯逻辑及Modal/战斗生命周期通过。

以上代数和控件桩验证不替代UrhoX实机像素验收；最终视觉一致性由Maker构建后的同状态截图确认。
