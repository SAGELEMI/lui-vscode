# LUI 2.5.1 本地验收

## 范围

- 共享契约统一 sRGB、Straight Alpha、预乘 sRGB 渐变、向内边框和阴影来源。
- Runtime 自有文本使用 MiSans 原字重与同色八方向光学覆盖补偿；原生组合控件明确标记 `native-raster`。
- Studio 禁止字体合成并等待字体加载完成；移除卡片、弹窗的 CSS 专用阴影。
- Runtime 增加稳定引用查找、布局矩形读取和全局覆盖层排序。

## 自动证据

- `npm test`：68 项通过。
- `npm run check:types`：通过。
- `tests/runtime-layout-math.py`：验证向内边框、阴影关闭、字体探针、引用查找及共享画刷调用。
- 游戏侧结构、页面、Modal/通知/教程生命周期和启动链夹具通过。

## 边界

本轮只执行本地构建、部署、截图夹具和 VSIX 打包，不安装或强制重载 VS Code，也不调用 Maker 远程构建。设备端 NanoVG 抗锯齿最终截图闭环留待后续获准构建。
# 2.5.1 补充验收：动态可见性

- 初始折叠的绑定节点保留 Runtime 实体并设为隐藏；字面量折叠节点仍可裁剪。
- LUI 单测 69/69，Lua 列表、属性、教程与布局代数夹具通过；游戏设置页验证名称编辑器点击后可见。
- 正式适配器重新盖章并部署至《无尽塔》，部署清单与源码一致；未触发 Maker 远程构建。
